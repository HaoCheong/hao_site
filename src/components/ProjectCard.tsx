const ProjectCard = ({ title, description, start_date, link }) => {
	return (
		<>
			<div class="bg-blue-300 w-full h-100 text-white font-jersey">
				PROJECT
			</div>
		</>
	);
};

export default ProjectCard;
